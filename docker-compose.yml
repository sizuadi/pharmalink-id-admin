services:
  admin:
    image: ${DOCKER_IMAGE:-sizuwanoadi/pharmalink-admin:latest}
    container_name: pharmalink-admin
    restart: unless-stopped
    env_file:
      - .env.docker
    ports:
      - "${ADMIN_PORT:-5173}:80"
